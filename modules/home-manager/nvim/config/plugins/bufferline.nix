{
  plugins = {
    bufferline = {
      enable = true;

      # Use settings instead of options
      settings = {
        mode = "tabs";
      };
    };

    # Enable required dependency
    web-devicons.enable = true;
  };
}
