{
  plugins = {
    # Enable and configure bufferline
    bufferline = {
      enable = true;

      # Main options
      options = {
        mode = "tabs";
      };
    };

    # Enable required dependency
    nvim-web-devicons = {
      enable = true;
    };
  };
}
