{
  plugins = {
    bufferline = {
      enable = true;

      # Use settings instead of options
      settings = {
        mode = "tabs";
      };
    };

    # Enable required dependency
    nvim-web-devicons.enable = true;
  };
}
